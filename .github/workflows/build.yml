name: Build and Test

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        build_type: [Debug, Release]
        compiler:
          - { name: gcc, cc: gcc, cxx: g++ }
          - { name: clang, cc: clang, cxx: clang++ }
          - { name: msvc, cc: cl, cxx: cl }
        exclude:
          # Windows doesn't have these compilers by default
          - os: windows-latest
            compiler:
              name: gcc
              cc: gcc
              cxx: g++
          - os: windows-latest
            compiler:
              name: clang
              cc: clang
              cxx: clang++
          # Linux doesn't have MSVC
          - os: ubuntu-latest
            compiler:
              name: msvc
              cc: cl
              cxx: cl

    runs-on: ${{ matrix.os }}
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup CMake
      uses: jwlawson/actions-setup-cmake@v1.14
      with:
        cmake-version: '3.20'

    - name: Install dependencies (Linux)
      if: matrix.os == 'ubuntu-latest'
      run: |
        sudo apt-get update
        sudo apt-get install -y git build-essential

    - name: Setup GCC (Linux)
      if: matrix.os == 'ubuntu-latest' && matrix.compiler.name == 'gcc'
      run: |
        sudo apt-get install -y gcc-10 g++-10
        echo "CC=gcc-10" >> $GITHUB_ENV
        echo "CXX=g++-10" >> $GITHUB_ENV

    - name: Setup Clang (Linux)
      if: matrix.os == 'ubuntu-latest' && matrix.compiler.name == 'clang'
      run: |
        sudo apt-get install -y clang-14
        echo "CC=clang-14" >> $GITHUB_ENV
        echo "CXX=clang++-14" >> $GITHUB_ENV

    - name: Install dependencies (Windows)
      if: matrix.os == 'windows-latest'
      run: |
        # Ensure Git is available for FetchContent
        git --version

    - name: Setup MSVC (Windows)
      if: matrix.os == 'windows-latest'
      uses: microsoft/setup-msbuild@v2

    - name: Cache build directory
      uses: actions/cache@v3
      with:
        path: build
        key: ${{ runner.os }}-${{ matrix.compiler.name }}-${{ matrix.build_type }}-${{ hashFiles('CMakeLists.txt') }}
        restore-keys: |
          ${{ runner.os }}-${{ matrix.compiler.name }}-${{ matrix.build_type }}-

    - name: Configure CMake (Linux GCC)
      if: matrix.os == 'ubuntu-latest' && matrix.compiler.name == 'gcc'
      run: |
        cmake -B build -S . -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} -DCMAKE_C_COMPILER=gcc-10 -DCMAKE_CXX_COMPILER=g++-10

    - name: Configure CMake (Linux Clang)
      if: matrix.os == 'ubuntu-latest' && matrix.compiler.name == 'clang'
      run: |
        cmake -B build -S . -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} -DCMAKE_C_COMPILER=clang-14 -DCMAKE_CXX_COMPILER=clang++-14

    - name: Configure CMake (Windows MSVC)
      if: matrix.os == 'windows-latest'
      run: |
        cmake -B build -S . -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} -G "Visual Studio 17 2022" -A x64

    - name: Build
      run: |
        cmake --build build --config ${{ matrix.build_type }}

    - name: Test
      run: |
        cd build
        ctest --build-config ${{ matrix.build_type }} --output-on-failure --verbose

    - name: Run main executable test
      shell: bash
      run: |
        if [ "${{ matrix.os }}" = "windows-latest" ]; then
          if [ -f "./build/${{ matrix.build_type }}/nova_genesis_orbitalguard_test.exe" ]; then
            ./build/${{ matrix.build_type }}/nova_genesis_orbitalguard_test.exe
          else
            echo "Main test executable not found, skipping..."
          fi
        else
          if [ -f "./build/nova_genesis_orbitalguard_test" ]; then
            ./build/nova_genesis_orbitalguard_test
          else
            echo "Main test executable not found, skipping..."
          fi
        fi

    - name: List build artifacts (Debug)
      if: failure()
      shell: bash
      run: |
        echo "Build directory contents:"
        if [ "${{ matrix.os }}" = "windows-latest" ]; then
          ls -la build/${{ matrix.build_type }}/
        else
          ls -la build/
        fi
